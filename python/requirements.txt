# Core dependencies
numpy>=1.24.0,<2.0.0
tqdm>=4.66.0
av>=16.1.0

# Audio transcription
faster-whisper>=1.2.1

# Color analysis
scikit-learn>=1.3.0

# OCR
easyocr>=1.7.0
Pillow>=9.3.0

# Monitoring
psutil>=5.9.0

# Websockets
websockets>=12.0
python-multipart>=0.0.6

# Environment
python-dotenv>=1.2.1

# ML/AI frameworks (CPU ONLY)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0
torchvision>=0.16.0

# If x86 (AMD64), use the tiny CPU-only version
tensorflow-cpu==2.15.0; platform_machine == "x86_64"
tensorflow==2.15.0; platform_machine == "aarch64"

# Face recognition (PyTorch backend)
deepface>=0.0.96
tf-keras

ultralytics>=8.0.0
transformers>=4.35.0
--index-url https://pypi.org/simple